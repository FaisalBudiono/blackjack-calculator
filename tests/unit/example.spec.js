import { shallowMount } from '@vue/test-utils';
import Card from '@/components/Card.vue';

describe('Example', () => {
  it('it just an example/boilerplate', () => {
    // const wrapper = shallowMount(HelloWorld, {
    //   propsData: { msg },
    // });

    // const wrapper = shallowMount(Card, {
    //   propsData: { id: 'asd' },
    // });

    // expect(wrapper).toBe.
  });
});
